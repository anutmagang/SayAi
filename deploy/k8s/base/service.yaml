apiVersion: v1
kind: Service
metadata:
  name: sayai-api
  labels:
    app.kubernetes.io/name: sayai-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sayai-api
  ports:
    - name: http
      port: 80
      targetPort: http
